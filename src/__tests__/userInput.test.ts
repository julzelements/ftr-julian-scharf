describe("user input", () => {
  it("should not accept a non integer", () => {});
  it("should set the refresh interval when supplied with a valid integer", () => {});
  it("should not accept", () => {});
  it("should not accept a non integer", () => {});
  it("should not accept a non integer", () => {});
});
